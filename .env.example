# cPanel Backend API Configuration
# After deploying cpanel_backend_api.zip to your cPanel subdomain,
# set the base URL here (no trailing slash)
VITE_CPANEL_API_BASE_URL=https://api.yourdomain.com

# Supabase Configuration (already in use for student auth/profile)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
